<header>
  <h1>メモアプリTop画面</h1>
  <section>
    <ul>
      <% @memos.each do |memo| %>
        <% title_name = memo['memo_title'] %>
        <li><a href="/memos/<%= memo['memo_id'] %>"><%= h title_name %></a></li>
      <% end %>
    </ul>
  </section>
</header>
